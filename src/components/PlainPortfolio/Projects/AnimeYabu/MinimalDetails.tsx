import React from 'react';

const MinimalDetails = () => {
    return (
        <p className="gray">
            A very simple single page web application to test out Firebase
            Authentication.
        </p>
    );
};

export default MinimalDetails;
